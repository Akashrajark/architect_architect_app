import 'package:flutter/material.dart';

class CreateHomePlanScreen extends StatelessWidget {
  const CreateHomePlanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
