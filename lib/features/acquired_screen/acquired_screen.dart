import 'package:flutter/material.dart';

class AcquiredScreen extends StatelessWidget {
  const AcquiredScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
